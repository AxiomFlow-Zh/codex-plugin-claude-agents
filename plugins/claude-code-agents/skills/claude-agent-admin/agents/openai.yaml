interface:
  display_name: "Multi-CLI Agents 配置"
  short_description: "配置 Runner、模型、网关、Dashboard 与排障"
  brand_color: "#10A37F"
  default_prompt: "检查并配置 Multi-CLI Agents 插件，优先使用 dry-run 和非秘密配置视图，不要直接执行开发委派任务。"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "multi_cli_agents"
      description: "本地 Multi-CLI Agents MCP server"
